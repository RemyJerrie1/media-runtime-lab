meta {
  name: Reject invalid command
  type: http
  seq: 4
}
post {
  url: {{baseUrl}}/v1/render-jobs
  body: json
  auth: none
}
body:json {
  {"projectId":"x","template":"portrait","durationSeconds":999,"narration":"","idempotencyKey":"short"}
}
tests {
  test("invalid contract is rejected", function () {
    expect(res.status).to.equal(400);
    expect(res.body.code).to.equal("INVALID_COMMAND");
  });
}
